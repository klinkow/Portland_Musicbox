<%= erb :header %>
<div class="container albums-container">

  <div class="col-sm-12">
    <% Artist.all.each do |artist| %>
      <div class="col-sm-4 artist_container">
        <a href="/artists/<%= artist.id %>"><h4><%= artist.name %></h4></a>
      </div>
    <% end %>
  </div>
</div>
